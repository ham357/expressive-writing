%header
  %nav
    .nav-wrapper
      = link_to root_path,class: "brand-logo",data: {"turbolinks" => false} do
        Logo
      %ul#nav-mobile.right.hide-on-med-and-down
        %li
          %a{:href => "sass.html"} Sass
        %li
          - if user_signed_in?
            = link_to "My Page", mypages_path,data: {"turbolinks" => false}
          - else
            = link_to "Sign up", new_user_registration_path,data: {"turbolinks" => false}
        %li
          - if user_signed_in?
            = link_to "Log out", destroy_user_session_path, method: :delete, data: { confirm: 'Do you want to log out?' } 
          - else
            = link_to "Log in", new_user_session_path,data: {"turbolinks" => false}
