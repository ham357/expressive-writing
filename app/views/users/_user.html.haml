%li.collection-item.avatar
  %span.title
    = check_user_icon(user)
    = link_to user_path(user.id) do
      = user.nickname
    = render 'relationships/follow_button', user: user
    %p
      = user.comment.present? ? user.comment : "はじめまして。宜しくお願いします。"
